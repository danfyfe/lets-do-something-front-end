const defaultState = {
  currentUser: {}
}


function appReducer(state = defaultState, action) {
  switch (action.type) {
    case "IDK":

      return state
    default:
      return state
  }
}




export default appReducer